{
  "name": "blando-spatial-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev --port 4321",
    "build": "npm run universe && astro build",
    "preview": "astro preview --port 4321",
    "universe": "node scripts/build-universe.mjs && node scripts/build-palette.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "astro": "^5.14.1",
    "three": "^0.181.0"
  },
  "devDependencies": {
    "js-yaml": "^5.3.0"
  }
}
